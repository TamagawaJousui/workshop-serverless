describe("process point", () => {
  test("trigger point process, except success", async () => {});
  test("trigger point process with wrong credential, except error", async () => {});
  test("trigger point process with token from another user rather than the admin, except error", async () => {});
});
